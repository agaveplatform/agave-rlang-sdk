# Agave Platform Science API
#
# Power your digital lab and reduce the time from theory to discovery using the Agave Science-as-a-Service API Platform. Agave provides hosted services that allow researchers to manage data, conduct experiments, and publish and share results from anywhere at any time.
#
# Agave Platform version: 2.2.14
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' FileMkdirAction Class
#'
#' Request for a new directory to be created on the target system. This will recursively create missing directories.
#'
#' @field action 
#' @field path Name of new directory or target file or folder.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FileMkdirAction <- R6::R6Class(
  'FileMkdirAction',
  public = list(
    `action` = NULL,
    `path` = NULL,
    initialize = function(`action`, `path`){
      if (!missing(`action`)) {
        stopifnot(R6::is.R6(`action`))
        self$`action` <- `action`
      }
      if (!missing(`path`)) {
        stopifnot(is.character(`path`), length(`path`) == 1)
        self$`path` <- `path`
      }
    },
    asJSON = function() {
      self$toJSON()
    },
    toJSON = function() {
      FileMkdirActionObject <- list()
      if (!is.null(self$`action`)) {
        FileMkdirActionObject[['action']] <- self$`action`$toJSON()
      }
      else {
        FileMkdirActionObject[['action']] <- NULL
      }
      if (!is.null(self$`path`)) {
        FileMkdirActionObject[['path']] <- self$`path`
      }
      else {
        FileMkdirActionObject[['path']] <- NULL
      }

      FileMkdirActionObject
    },
    fromJSON = function(FileMkdirActionObject) {
      if (is.character(FileMkdirActionObject)) {
        FileMkdirActionObject <- jsonlite::fromJSON(FileMkdirActionJson)
      }

      if ("result" %in% names(FileMkdirActionObject)) {
        FileMkdirActionObject <- FileMkdirActionObject$result
      }

      if (!is.null(FileMkdirActionObject$`action`)) {
        actionObject <- FileManagementActionType$new()
        actionObject$fromJSON(jsonlite::toJSON(FileMkdirActionObject$action, auto_unbox = TRUE))
        self$`action` <- actionObject
      }
      if (!is.null(FileMkdirActionObject$`path`)) {
        self$`path` <- FileMkdirActionObject$`path`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "action": %s,
           "path": %s
        }',
        self$`action`$toJSON(),
        ifelse( is.null(self$`path`),"null",paste0(c('"', self$`path`, '"')))
      )
    },
    fromJSONString = function(FileMkdirActionJson) {
      FileMkdirActionObject <- jsonlite::fromJSON(FileMkdirActionJson)
      self::fromJSON(FileMkdirActionObject)

    }
  )
)
