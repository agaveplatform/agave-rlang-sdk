# Agave Platform Science API
#
# Power your digital lab and reduce the time from theory to discovery using the Agave Science-as-a-Service API Platform. Agave provides hosted services that allow researchers to manage data, conduct experiments, and publish and share results from anywhere at any time.
#
# Agave Platform version: 2.2.14
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' SystemAction Class
#'
#' A generic action to apply to a system
#'
#' @field action 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SystemAction <- R6::R6Class(
  'SystemAction',
  public = list(
    `action` = NULL,
    initialize = function(`action`){
      if (!missing(`action`)) {
        stopifnot(R6::is.R6(`action`))
        self$`action` <- `action`
      }
    },
    asJSON = function() {
      self$toJSON()
    },
    toJSON = function() {
      SystemActionObject <- list()
      if (!is.null(self$`action`)) {
        SystemActionObject[['action']] <- self$`action`$toJSON()
      }
      else {
        SystemActionObject[['action']] <- NULL
      }

      SystemActionObject
    },
    fromJSON = function(SystemActionObject) {
      if (is.character(SystemActionObject)) {
        SystemActionObject <- jsonlite::fromJSON(SystemActionJson)
      }

      if ("result" %in% names(SystemActionObject)) {
        SystemActionObject <- SystemActionObject$result
      }

      if (!is.null(SystemActionObject$`action`)) {
        actionObject <- SystemActionType$new()
        actionObject$fromJSON(jsonlite::toJSON(SystemActionObject$action, auto_unbox = TRUE))
        self$`action` <- actionObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "action": %s
        }',
        self$`action`$toJSON()
      )
    },
    fromJSONString = function(SystemActionJson) {
      SystemActionObject <- jsonlite::fromJSON(SystemActionJson)
      self::fromJSON(SystemActionObject)

    }
  )
)
