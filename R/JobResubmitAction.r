# Agave Platform Science API
#
# Power your digital lab and reduce the time from theory to discovery using the Agave Science-as-a-Service API Platform. Agave provides hosted services that allow researchers to manage data, conduct experiments, and publish and share results from anywhere at any time.
#
# Agave Platform version: 2.2.14
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' JobResubmitAction Class
#'
#' Resubmits the given job as a new job with a unique id. The archive system and path are reset. All other fields remain unchanged. Notifications set for the original job will not carry over.
#'
#' @field action 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
JobResubmitAction <- R6::R6Class(
  'JobResubmitAction',
  public = list(
    `action` = NULL,
    initialize = function(`action`){
      if (!missing(`action`)) {
        stopifnot(R6::is.R6(`action`))
        self$`action` <- `action`
      }
    },
    asJSON = function() {
      self$toJSON()
    },
    toJSON = function() {
      JobResubmitActionObject <- list()
      if (!is.null(self$`action`)) {
        JobResubmitActionObject[['action']] <- self$`action`$toJSON()
      }
      else {
        JobResubmitActionObject[['action']] <- NULL
      }

      JobResubmitActionObject
    },
    fromJSON = function(JobResubmitActionObject) {
      if (is.character(JobResubmitActionObject)) {
        JobResubmitActionObject <- jsonlite::fromJSON(JobResubmitActionJson)
      }

      if ("result" %in% names(JobResubmitActionObject)) {
        JobResubmitActionObject <- JobResubmitActionObject$result
      }

      if (!is.null(JobResubmitActionObject$`action`)) {
        actionObject <- JobActionType$new()
        actionObject$fromJSON(jsonlite::toJSON(JobResubmitActionObject$action, auto_unbox = TRUE))
        self$`action` <- actionObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "action": %s
        }',
        self$`action`$toJSON()
      )
    },
    fromJSONString = function(JobResubmitActionJson) {
      JobResubmitActionObject <- jsonlite::fromJSON(JobResubmitActionJson)
      self::fromJSON(JobResubmitActionObject)

    }
  )
)
