# Agave Platform Science API
#
# Power your digital lab and reduce the time from theory to discovery using the Agave Science-as-a-Service API Platform. Agave provides hosted services that allow researchers to manage data, conduct experiments, and publish and share results from anywhere at any time.
#
# Agave Platform version: 2.2.14
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' System Class
#'
#' 
#'
#' @field description Verbose description of this system.
#' @field environment Environment variables to set upon login prior to job submission.
#' @field id Unique identifier for this system.
#' @field isDefault Is the system the default for the authenticated user?
#' @field isPublic Is the system publicly available?
#' @field lastModified The date this system was last modified in ISO 8601 format.
#' @field name Common name for this system.
#' @field revision The number of times this app has been updated.
#' @field scratchDir The scratch directory where job execution directories will be created at runtime. The workDir is used if this is not specified.
#' @field site The site associated with this system.
#' @field status 
#' @field storage 
#' @field type 
#' @field uuid The uuid of this system.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
System <- R6::R6Class(
  'System',
  public = list(
    `description` = NULL,
    `environment` = NULL,
    `id` = NULL,
    `isDefault` = NULL,
    `isPublic` = NULL,
    `lastModified` = NULL,
    `name` = NULL,
    `revision` = NULL,
    `scratchDir` = NULL,
    `site` = NULL,
    `status` = NULL,
    `storage` = NULL,
    `type` = NULL,
    `uuid` = NULL,
    initialize = function(`description`, `environment`, `id`, `isDefault`, `isPublic`, `lastModified`, `name`, `revision`, `scratchDir`, `site`, `status`, `storage`, `type`, `uuid`){
      if (!missing(`description`)) {
        stopifnot(is.character(`description`), length(`description`) == 1)
        self$`description` <- `description`
      }
      if (!missing(`environment`)) {
        stopifnot(is.character(`environment`), length(`environment`) == 1)
        self$`environment` <- `environment`
      }
      if (!missing(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`isDefault`)) {
        self$`isDefault` <- `isDefault`
      }
      if (!missing(`isPublic`)) {
        self$`isPublic` <- `isPublic`
      }
      if (!missing(`lastModified`)) {
        stopifnot(is.character(`lastModified`), length(`lastModified`) == 1)
        self$`lastModified` <- `lastModified`
      }
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!missing(`revision`)) {
        stopifnot(is.numeric(`revision`), length(`revision`) == 1)
        self$`revision` <- `revision`
      }
      if (!missing(`scratchDir`)) {
        stopifnot(is.character(`scratchDir`), length(`scratchDir`) == 1)
        self$`scratchDir` <- `scratchDir`
      }
      if (!missing(`site`)) {
        stopifnot(is.character(`site`), length(`site`) == 1)
        self$`site` <- `site`
      }
      if (!missing(`status`)) {
        stopifnot(R6::is.R6(`status`))
        self$`status` <- `status`
      }
      if (!missing(`storage`)) {
        stopifnot(R6::is.R6(`storage`))
        self$`storage` <- `storage`
      }
      if (!missing(`type`)) {
        stopifnot(R6::is.R6(`type`))
        self$`type` <- `type`
      }
      if (!missing(`uuid`)) {
        stopifnot(is.character(`uuid`), length(`uuid`) == 1)
        self$`uuid` <- `uuid`
      }
    },
    asJSON = function() {
      self$toJSON()
    },
    toJSON = function() {
      SystemObject <- list()
      if (!is.null(self$`description`)) {
        SystemObject[['description']] <- self$`description`
      }
      else {
        SystemObject[['description']] <- NULL
      }
      if (!is.null(self$`environment`)) {
        SystemObject[['environment']] <- self$`environment`
      }
      else {
        SystemObject[['environment']] <- NULL
      }
      if (!is.null(self$`id`)) {
        SystemObject[['id']] <- self$`id`
      }
      else {
        SystemObject[['id']] <- NULL
      }
      if (!is.null(self$`isDefault`)) {
        SystemObject[['isDefault']] <- self$`isDefault`
      }
      else {
        SystemObject[['isDefault']] <- NULL
      }
      if (!is.null(self$`isPublic`)) {
        SystemObject[['isPublic']] <- self$`isPublic`
      }
      else {
        SystemObject[['isPublic']] <- NULL
      }
      if (!is.null(self$`lastModified`)) {
        SystemObject[['lastModified']] <- self$`lastModified`
      }
      else {
        SystemObject[['lastModified']] <- NULL
      }
      if (!is.null(self$`name`)) {
        SystemObject[['name']] <- self$`name`
      }
      else {
        SystemObject[['name']] <- NULL
      }
      if (!is.null(self$`revision`)) {
        SystemObject[['revision']] <- self$`revision`
      }
      else {
        SystemObject[['revision']] <- NULL
      }
      if (!is.null(self$`scratchDir`)) {
        SystemObject[['scratchDir']] <- self$`scratchDir`
      }
      else {
        SystemObject[['scratchDir']] <- NULL
      }
      if (!is.null(self$`site`)) {
        SystemObject[['site']] <- self$`site`
      }
      else {
        SystemObject[['site']] <- NULL
      }
      if (!is.null(self$`status`)) {
        SystemObject[['status']] <- self$`status`$toJSON()
      }
      else {
        SystemObject[['status']] <- NULL
      }
      if (!is.null(self$`storage`)) {
        SystemObject[['storage']] <- self$`storage`$toJSON()
      }
      else {
        SystemObject[['storage']] <- NULL
      }
      if (!is.null(self$`type`)) {
        SystemObject[['type']] <- self$`type`$toJSON()
      }
      else {
        SystemObject[['type']] <- NULL
      }
      if (!is.null(self$`uuid`)) {
        SystemObject[['uuid']] <- self$`uuid`
      }
      else {
        SystemObject[['uuid']] <- NULL
      }

      SystemObject
    },
    fromJSON = function(SystemObject) {
      if (is.character(SystemObject)) {
        SystemObject <- jsonlite::fromJSON(SystemJson)
      }

      if ("result" %in% names(SystemObject)) {
        SystemObject <- SystemObject$result
      }

      if (!is.null(SystemObject$`description`)) {
        self$`description` <- SystemObject$`description`
      }
      if (!is.null(SystemObject$`environment`)) {
        self$`environment` <- SystemObject$`environment`
      }
      if (!is.null(SystemObject$`id`)) {
        self$`id` <- SystemObject$`id`
      }
      if (!is.null(SystemObject$`isDefault`)) {
        self$`isDefault` <- SystemObject$`isDefault`
      }
      if (!is.null(SystemObject$`isPublic`)) {
        self$`isPublic` <- SystemObject$`isPublic`
      }
      if (!is.null(SystemObject$`lastModified`)) {
        self$`lastModified` <- SystemObject$`lastModified`
      }
      if (!is.null(SystemObject$`name`)) {
        self$`name` <- SystemObject$`name`
      }
      if (!is.null(SystemObject$`revision`)) {
        self$`revision` <- SystemObject$`revision`
      }
      if (!is.null(SystemObject$`scratchDir`)) {
        self$`scratchDir` <- SystemObject$`scratchDir`
      }
      if (!is.null(SystemObject$`site`)) {
        self$`site` <- SystemObject$`site`
      }
      if (!is.null(SystemObject$`status`)) {
        statusObject <- SystemStatusType$new()
        statusObject$fromJSON(jsonlite::toJSON(SystemObject$status, auto_unbox = TRUE))
        self$`status` <- statusObject
      }
      if (!is.null(SystemObject$`storage`)) {
        storageObject <- SystemStorageConfig$new()
        storageObject$fromJSON(jsonlite::toJSON(SystemObject$storage, auto_unbox = TRUE))
        self$`storage` <- storageObject
      }
      if (!is.null(SystemObject$`type`)) {
        typeObject <- SystemType$new()
        typeObject$fromJSON(jsonlite::toJSON(SystemObject$type, auto_unbox = TRUE))
        self$`type` <- typeObject
      }
      if (!is.null(SystemObject$`uuid`)) {
        self$`uuid` <- SystemObject$`uuid`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "description": %s,
           "environment": %s,
           "id": %s,
           "isDefault": %s,
           "isPublic": %s,
           "lastModified": %s,
           "name": %s,
           "revision": %d,
           "scratchDir": %s,
           "site": %s,
           "status": %s,
           "storage": %s,
           "type": %s,
           "uuid": %s
        }',
        ifelse( is.null(self$`description`),"null",paste0(c('"', self$`description`, '"'))),
        ifelse( is.null(self$`environment`),"null",paste0(c('"', self$`environment`, '"'))),
        ifelse( is.null(self$`id`),"null",paste0(c('"', self$`id`, '"'))),
        ifelse( is.null(self$`isDefault`),"null",paste0(c('"', self$`isDefault`, '"'))),
        ifelse( is.null(self$`isPublic`),"null",paste0(c('"', self$`isPublic`, '"'))),
        ifelse( is.null(self$`lastModified`),"null",paste0(c('"', self$`lastModified`, '"'))),
        ifelse( is.null(self$`name`),"null",paste0(c('"', self$`name`, '"'))),
        ifelse( is.null(self$`revision`),"null",paste0(c('"', self$`revision`, '"'))),
        ifelse( is.null(self$`scratchDir`),"null",paste0(c('"', self$`scratchDir`, '"'))),
        ifelse( is.null(self$`site`),"null",paste0(c('"', self$`site`, '"'))),
        self$`status`$toJSON(),
        self$`storage`$toJSON(),
        self$`type`$toJSON(),
        ifelse( is.null(self$`uuid`),"null",paste0(c('"', self$`uuid`, '"')))
      )
    },
    fromJSONString = function(SystemJson) {
      SystemObject <- jsonlite::fromJSON(SystemJson)
      self::fromJSON(SystemObject)

    }
  )
)
