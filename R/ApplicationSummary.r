# Agave Platform Science API
#
# Power your digital lab and reduce the time from theory to discovery using the Agave Science-as-a-Service API Platform. Agave provides hosted services that allow researchers to manage data, conduct experiments, and publish and share results from anywhere at any time.
#
# Agave Platform version: 2.2.14
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git


#' ApplicationSummary Class
#'
#' An abbreviated Application object returned in request in application collection queries
#'
#' @field id Unique id of this app. Comprised of the app name-version.
#' @field executionSystem The system id of the execution system where this app should run.
#' @field lastModified The date this application was last modified in ISO 8601 format.
#' @field name The name of the application. The name does not have to be unique, but the combination of name and version does.
#' @field isPublic Whether the application is public or private.
#' @field revision The number of times this application has been revised.
#' @field shortDescription The short description of this application.
#' @field version The version of the application in #.#.# format. While the version does not need to be unique, the combination of name and version does have to be unique.
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
ApplicationSummary <- R6::R6Class(
  'ApplicationSummary',
  public = list(
    `id` = NULL,
    `executionSystem` = NULL,
    `lastModified` = NULL,
    `name` = NULL,
    `isPublic` = NULL,
    `revision` = NULL,
    `shortDescription` = NULL,
    `version` = NULL,
    initialize = function(`id`, `executionSystem`, `lastModified`, `name`, `isPublic`, `revision`, `shortDescription`, `version`){
      if (!missing(`id`)) {
        stopifnot(is.character(`id`), length(`id`) == 1)
        self$`id` <- `id`
      }
      if (!missing(`executionSystem`)) {
        stopifnot(is.character(`executionSystem`), length(`executionSystem`) == 1)
        self$`executionSystem` <- `executionSystem`
      }
      if (!missing(`lastModified`)) {
        stopifnot(is.character(`lastModified`), length(`lastModified`) == 1)
        self$`lastModified` <- `lastModified`
      }
      if (!missing(`name`)) {
        stopifnot(is.character(`name`), length(`name`) == 1)
        self$`name` <- `name`
      }
      if (!missing(`isPublic`)) {
        self$`isPublic` <- `isPublic`
      }
      if (!missing(`revision`)) {
        stopifnot(is.numeric(`revision`), length(`revision`) == 1)
        self$`revision` <- `revision`
      }
      if (!missing(`shortDescription`)) {
        stopifnot(is.character(`shortDescription`), length(`shortDescription`) == 1)
        self$`shortDescription` <- `shortDescription`
      }
      if (!missing(`version`)) {
        stopifnot(is.character(`version`), length(`version`) == 1)
        self$`version` <- `version`
      }
    },
    asJSON = function() {
      self$toJSON()
    },
    toJSON = function() {
      ApplicationSummaryObject <- list()
      if (!is.null(self$`id`)) {
        ApplicationSummaryObject[['id']] <- self$`id`
      }
      else {
        ApplicationSummaryObject[['id']] <- NULL
      }
      if (!is.null(self$`executionSystem`)) {
        ApplicationSummaryObject[['executionSystem']] <- self$`executionSystem`
      }
      else {
        ApplicationSummaryObject[['executionSystem']] <- NULL
      }
      if (!is.null(self$`lastModified`)) {
        ApplicationSummaryObject[['lastModified']] <- self$`lastModified`
      }
      else {
        ApplicationSummaryObject[['lastModified']] <- NULL
      }
      if (!is.null(self$`name`)) {
        ApplicationSummaryObject[['name']] <- self$`name`
      }
      else {
        ApplicationSummaryObject[['name']] <- NULL
      }
      if (!is.null(self$`isPublic`)) {
        ApplicationSummaryObject[['isPublic']] <- self$`isPublic`
      }
      else {
        ApplicationSummaryObject[['isPublic']] <- NULL
      }
      if (!is.null(self$`revision`)) {
        ApplicationSummaryObject[['revision']] <- self$`revision`
      }
      else {
        ApplicationSummaryObject[['revision']] <- NULL
      }
      if (!is.null(self$`shortDescription`)) {
        ApplicationSummaryObject[['shortDescription']] <- self$`shortDescription`
      }
      else {
        ApplicationSummaryObject[['shortDescription']] <- NULL
      }
      if (!is.null(self$`version`)) {
        ApplicationSummaryObject[['version']] <- self$`version`
      }
      else {
        ApplicationSummaryObject[['version']] <- NULL
      }

      ApplicationSummaryObject
    },
    fromJSON = function(ApplicationSummaryObject) {
      if (is.character(ApplicationSummaryObject)) {
        ApplicationSummaryObject <- jsonlite::fromJSON(ApplicationSummaryJson)
      }

      if ("result" %in% names(ApplicationSummaryObject)) {
        ApplicationSummaryObject <- ApplicationSummaryObject$result
      }

      if (!is.null(ApplicationSummaryObject$`id`)) {
        self$`id` <- ApplicationSummaryObject$`id`
      }
      if (!is.null(ApplicationSummaryObject$`executionSystem`)) {
        self$`executionSystem` <- ApplicationSummaryObject$`executionSystem`
      }
      if (!is.null(ApplicationSummaryObject$`lastModified`)) {
        self$`lastModified` <- ApplicationSummaryObject$`lastModified`
      }
      if (!is.null(ApplicationSummaryObject$`name`)) {
        self$`name` <- ApplicationSummaryObject$`name`
      }
      if (!is.null(ApplicationSummaryObject$`isPublic`)) {
        self$`isPublic` <- ApplicationSummaryObject$`isPublic`
      }
      if (!is.null(ApplicationSummaryObject$`revision`)) {
        self$`revision` <- ApplicationSummaryObject$`revision`
      }
      if (!is.null(ApplicationSummaryObject$`shortDescription`)) {
        self$`shortDescription` <- ApplicationSummaryObject$`shortDescription`
      }
      if (!is.null(ApplicationSummaryObject$`version`)) {
        self$`version` <- ApplicationSummaryObject$`version`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "id": %s,
           "executionSystem": %s,
           "lastModified": %s,
           "name": %s,
           "isPublic": %s,
           "revision": %d,
           "shortDescription": %s,
           "version": %s
        }',
        ifelse( is.null(self$`id`),"null",paste0(c('"', self$`id`, '"'))),
        ifelse( is.null(self$`executionSystem`),"null",paste0(c('"', self$`executionSystem`, '"'))),
        ifelse( is.null(self$`lastModified`),"null",paste0(c('"', self$`lastModified`, '"'))),
        ifelse( is.null(self$`name`),"null",paste0(c('"', self$`name`, '"'))),
        ifelse( is.null(self$`isPublic`),"null",paste0(c('"', self$`isPublic`, '"'))),
        ifelse( is.null(self$`revision`),"null",paste0(c('"', self$`revision`, '"'))),
        ifelse( is.null(self$`shortDescription`),"null",paste0(c('"', self$`shortDescription`, '"'))),
        ifelse( is.null(self$`version`),"null",paste0(c('"', self$`version`, '"')))
      )
    },
    fromJSONString = function(ApplicationSummaryJson) {
      ApplicationSummaryObject <- jsonlite::fromJSON(ApplicationSummaryJson)
      self::fromJSON(ApplicationSummaryObject)

    }
  )
)
